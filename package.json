{
  "scripts": {
    "start": "npm run build-watch & npm run server",
    "build-watch": "webpack -d --watch",
    "server": "browser-sync start --server --no-notify"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.1",
    "browser-sync": "^2.23.1",
    "browser-sync-webpack-plugin": "^1.2.0",
    "lodash": "^4.17.4",
    "webpack": "^3.10.0"
  }
}
